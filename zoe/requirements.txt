# ZOE v1.0 — Requirements
# Core
aiohttp>=3.9.0
asyncio>=3.4.3

# Tests
pytest>=7.4.0
pytest-asyncio>=0.23.0

# LLM peripherals (opcional según backend)
# Para OllamaPeripheral: no requiere extras (usa aiohttp)
# Para OpenAICompatiblePeripheral: no requiere extras (usa aiohttp)
# Para ZAIPeripheral: requiere z-ai CLI en PATH

# Memory (Fase 3)
# networkx>=3.2
# sentence-transformers>=2.2.0

# Active Inference (Fase 2)
# pymdp>=0.0.5

# Crypto (Fase 1)
# cryptography>=42.0.0
