# ZOE V1.2 — Synthetic Cognitive Organism (SCO)
# Requirements
# https://github.com/fernandofondillo/ZOE-Organismo-Cognitivo-Sintetico-SCO

# Core async
aiohttp>=3.9.0

# Numerical computation
numpy>=1.24.0

# System monitoring
psutil>=5.9.0

# YAML config (use cases, capsule metadata)
PyYAML>=6.0

# Dev / test dependencies
pytest>=7.4.0
pytest-asyncio>=0.23.0

# LLM peripherals (optional, choose your backend):
# - OllamaPeripheral: requires Ollama running locally (https://ollama.ai)
# - OpenAICompatiblePeripheral: requires API key
# - ZAIPeripheral: requires z-ai CLI in PATH
# - MockPeripheral: no extras needed (for tests/development)

# Optional advanced features (uncomment to enable):
# Memory embeddings: sentence-transformers>=2.2.0
# Active Inference: pymdp>=0.0.5
# Crypto: cryptography>=42.0.0
